<Window x:Class="WhisperKey.TranscriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhisperKey"
        mc:Ignorable="d"
        Title="WhisperKey - Voice Dictation" 
        Height="400" Width="600"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Topmost="True"
        ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header with status and controls -->
        <Border Grid.Row="0" Background="{DynamicResource DarkBackgroundBrush}" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Status indicator -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Name="StatusIndicator" Width="12" Height="12" Fill="{Binding StatusIndicatorFill}" Margin="0,0,8,0"
                             AutomationProperties.Name="Recording Status Indicator"
                             AutomationProperties.HelpText="Visual indicator showing recording status. Green indicates ready/listening state."/>
                    <TextBlock Name="StatusText" Text="{Binding StatusText}" VerticalAlignment="Center" FontSize="12"
                               AutomationProperties.Name="Recording Status Text"/>
                </StackPanel>
                
                <!-- Title -->
                <TextBlock Grid.Column="1" Text="Voice Dictation" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                
                <!-- Close button -->
                <Button Grid.Column="2" Name="CloseButton" Content="✕" Width="24" Height="24"
                        Background="Transparent" BorderThickness="0"
                        Command="{Binding CloseCommand}" FontSize="14" Cursor="Hand"
                        AutomationProperties.Name="Close Window"
                        AutomationProperties.HelpText="Close transcription window and return to main interface"/>
            </Grid>
        </Border>
        
        <!-- Main transcription text area -->
        <ScrollViewer Grid.Row="1" Name="TextScrollViewer" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled" Padding="15,10"
                      AutomationProperties.Name="Transcription Text Area">
            <TextBlock Name="TranscriptionText"
                       Text="{Binding TranscriptionText}"
                       TextWrapping="Wrap"
                       FontFamily="Segoe UI"
                       FontSize="14"
                       LineHeight="20"
                       Foreground="#333333"
                       AutomationProperties.Name="Transcribed Text"
                       AutomationProperties.LiveSetting="Assertive"/>
        </ScrollViewer>
        
        <!-- Footer with usage info -->
        <Border Grid.Row="2" Background="#F8F8F8" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Usage info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Name="UsageText" Text="{Binding UsageText}" FontSize="10" Foreground="#666666"/>
                </StackPanel>
                
                <!-- Settings hint -->
                <TextBlock Grid.Column="1" Text="Press Escape to close" FontSize="10" Foreground="#999999" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
