---
phase: 07-msi-installer
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - tests/InstallerTests/InstallerTests.csproj
  - tests/InstallerTests/MsiValidator.cs
  - tests/InstallerTests/InstallTests.cs
  - tests/InstallerTests/UninstallTests.cs
  - scripts/test-installer.ps1
  - scripts/validate-msi.ps1
autonomous: true
must_haves:
  truths:
    - Automated tests verify MSI installation succeeds
    - Uninstall leaves system in clean state
    - ICE validation passes without errors
    - Upgrade scenario tested (v1.0 → v1.1)
    - Log files are validated for completeness
  artifacts:
    - path: tests/InstallerTests/InstallerTests.csproj
      provides: MSTest project for MSI validation
      min_lines: 40
    - path: tests/InstallerTests/MsiValidator.cs
      provides: ICE validation and MSI inspection
      min_lines: 150
    - path: scripts/test-installer.ps1
      provides: PowerShell test automation
      min_lines: 120
    - path: scripts/validate-msi.ps1
      provides: MSI validation script
      min_lines: 100
  key_links:
    - from: test-installer.ps1
      to: WhisperKey.Setup.msi
      via: msiexec.exe
      pattern: "msiexec.*WhisperKey"
    - from: MsiValidator
      to: Windows Installer API
      via: Dtf.WindowsInstaller
      pattern: "Database.*Open|View.*Execute"
---

<objective>
Create comprehensive testing infrastructure for the MSI installer. Test installation, uninstallation, upgrade scenarios, and validate MSI package integrity.

Purpose: Ensure the installer works correctly in all scenarios and produces clean results
Output: Automated test suite that validates MSI behavior and log quality
</objective>

<execution_context>
@C:/Users/samar/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-msi-installer/07-02-SUMMARY.md
@.planning/phases/07-msi-installer/07-03-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MSI test project with validation utilities</name>
  <files>tests/InstallerTests/InstallerTests.csproj, tests/InstallerTests/MsiValidator.cs</files>
  <action>
Create a test project for MSI validation:

**InstallerTests.csproj:**
1. Create MSTest v3 project targeting .NET 8.0
2. Add NuGet packages:
   - WixToolset.Dtf.WindowsInstaller (for MSI API access)
   - Microsoft.PowerShell.SDK (for PowerShell integration)
   - System.Management.Automation
3. Reference the WiX project for build dependency
4. Configure test settings for admin privileges (required for MSI testing)

**MsiValidator.cs:**
Create utility class with methods:
- ValidateIceRules(msiPath) - Run ICE validation
- InspectDatabase(msiPath) - Check MSI tables
- VerifyComponents(msiPath) - Validate component integrity
- CheckUpgradeLogic(msiPath) - Verify upgrade code handling
- ValidateShortcuts(msiPath) - Check shortcut targets
- VerifyRegistryEntries(msiPath) - Validate registry data
- CheckFilePermissions(msiPath) - Verify ACL settings

Include helper methods:
- OpenMsiDatabase(msiPath, mode)
- ExecuteQuery(database, query)
- GetPropertyValue(database, propertyName)
- GetTableRows(database, tableName)

Use Windows Installer API through Dtf (Deployment Tools Foundation).
  </action>
  <verify>dotnet build tests/InstallerTests/InstallerTests.csproj 2>&1 | grep -E "(error|Build succeeded)" | head -3</verify>
  <done>InstallerTests project builds with MsiValidator class</done>
</task>

<task type="auto">
  <name>Task 2: Implement installation and uninstallation tests</name>
  <files>tests/InstallerTests/InstallTests.cs, tests/InstallerTests/UninstallTests.cs</files>
  <action>
Create comprehensive install/uninstall test classes:

**InstallTests.cs:**
Test methods:
- Install_FreshInstall_Succeeds() - Basic install
- Install_WithLogging_ProducesLog() - Verify log creation
- Install_SilentMode_Succeeds() - /qn install
- Install_WithAllFeatures_InstallsFiles() - Complete install
- Install_InvalidProductCode_Fails() - Error handling
- Install_MissingPrerequisites_ShowsError() - Pre-req check
- Install_CreatesRegistryEntries() - Registry validation
- Install_CreatesShortcuts() - Shortcut validation
- Install_SetsCorrectPermissions() - ACL validation
- Install_CreatesLogWithExpectedContent() - Log content check

**UninstallTests.cs:**
Test methods:
- Uninstall_AfterInstall_RemovesAllFiles() - Complete removal
- Uninstall_RemovesRegistryEntries() - Registry cleanup
- Uninstall_RemovesShortcuts() - Shortcut cleanup
- Uninstall_LeavesSystemClean() - No orphaned data
- Uninstall_ProducesUninstallLog() - Logging verified
- Uninstall_PreservesUserData() - Optional data retention
- Uninstall_CanBeReinstalled() - Re-install after removal

Use TestInitialize/TestCleanup to manage VM state or temp directories.
Mark tests requiring admin with [TestCategory("AdminRequired")].
Use MSTest data-driven tests for parameter variations.
  </action>
  <verify>grep -c "\[TestMethod\]" tests/InstallerTests/InstallTests.cs tests/InstallerTests/UninstallTests.cs | awk -F: '{sum+=$2} END {print (sum >= 10 ? "pass" : "fail")}'</verify>
  <done>At least 10 test methods across install and uninstall tests</done>
</task>

<task type="auto">
  <name>Task 3: Create PowerShell automation for installer testing</name>
  <files>scripts/test-installer.ps1</files>
  <action>
Create `scripts/test-installer.ps1` for comprehensive installer testing:

1. Parameters:
   - $MsiPath (path to MSI file)
   - $TestMode (Full, InstallOnly, UninstallOnly, Upgrade)
   - $LogDir (where to save test logs)
   - $KeepLogs (switch to preserve logs)
   - $VerboseLogging (switch for extra verbose)

2. Test phases:
   - Pre-flight checks (MSI exists, admin rights)
   - Clean environment (uninstall if present)
   - Installation test with logging
   - Post-install validation (files, registry, shortcuts)
   - Application launch test (if applicable)
   - Upgrade test (install over existing)
   - Uninstallation test
   - Post-uninstall validation (clean system)

3. Log management:
   - Capture MSI logs for each operation
   - Parse logs for errors and warnings
   - Generate summary report
   - Save logs with timestamps

4. Validation checks:
   - Exit code verification (0 = success)
   - File existence after install
   - Registry key presence
   - Service status (if applicable)
   - Log file completeness

5. Output:
   - Console progress reporting
   - JSON test results file
   - Exit code (0 = all passed, 1 = any failed)

Include comment-based help for documentation.
  </action>
  <verify>grep -c "function Test-" scripts/test-installer.ps1 | awk '{print ($1 >= 3 ? "pass" : "fail")}'</verify>
  <done>test-installer.ps1 has at least 3 test functions</done>
</task>

<task type="auto">
  <name>Task 4: Create MSI validation script for ICE rules</name>
  <files>scripts/validate-msi.ps1</files>
  <action>
Create `scripts/validate-msi.ps1` for MSI package validation:

1. Parameters:
   - $MsiPath (path to MSI file)
   - $IceLevel (Error, Warning, Info, All)
   - $OutputFormat (Console, JSON, HTML)
   - $FailOnWarning (switch)

2. Validation checks:
   - ICE01 (Simple validation)
   - ICE03 (Data type validation)
   - ICE06 (Component rule validation)
   - ICE18 (KeyPath validation)
   - ICE25 (Component validation)
   - ICE33 (Registry validation)
   - ICE38 (Component/install location)
   - ICE43 (Component keypath)
   - ICE57 (Component/file validation)
   - ICE64 (Directory validation)

3. Custom validations:
   - Verify UpgradeCode is valid GUID
   - Check ProductVersion format
   - Validate all paths are proper MSI formatted
   - Verify no hardcoded paths
   - Check for consistent naming

4. Report generation:
   - Summary of passed/failed checks
   - Detailed error messages
   - Suggestions for fixes
   - Risk assessment (Critical/High/Medium/Low)

5. Integration:
   - Can be called from CI/CD pipeline
   - Returns exit code 0 for success, 1 for failures
   - Produces artifacts for build reports

Use Windows Installer API or Orca automation for ICE validation.
  </action>
  <verify>grep -c "ICE" scripts/validate-msi.ps1 | awk '{print ($1 >= 5 ? "pass" : "fail")}'</verify>
  <done>validate-msi.ps1 includes at least 5 ICE validation checks</done>
</task>

</tasks>

<verification>
- [ ] InstallerTests project builds successfully
- [ ] MsiValidator can open and inspect MSI databases
- [ ] test-installer.ps1 runs full install/uninstall cycle
- [ ] validate-msi.ps1 checks ICE rules
- [ ] Tests can be run from command line
- [ ] Test logs are generated and readable
</verification>

<success_criteria>
- Automated tests for install/uninstall scenarios
- ICE validation script validates MSI integrity
- PowerShell automation for CI/CD integration
- Test coverage for upgrade scenarios
- Log validation ensures logging works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-msi-installer/07-04-SUMMARY.md`
</output>
