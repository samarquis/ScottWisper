---
phase: 07-msi-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .wix/WhisperKey.Setup.wixproj
  - .wix/WhisperKey.Setup.wxs
  - .wix/Variables.wxi
  - .wix/Config.wxi
  - Directory.Build.props
autonomous: true
must_haves:
  truths:
    - WiX Toolset v5 SDK is integrated into the solution
    - MSI project builds without errors
    - Basic installer produces a valid MSI file
    - Installer can be run and produces exit code
  artifacts:
    - path: .wix/WhisperKey.Setup.wixproj
      provides: WiX project file with SDK references
      min_lines: 40
    - path: .wix/WhisperKey.Setup.wxs
      provides: Main WiX source with product definition
      min_lines: 100
    - path: .wix/Variables.wxi
      provides: Version and build variables
      min_lines: 20
    - path: .wix/Config.wxi
      provides: Conditional compilation symbols
      min_lines: 15
  key_links:
    - from: WhisperKey.Setup.wixproj
      to: WhisperKey.Main.csproj
      via: ProjectReference
      pattern: "ProjectReference.*Main"
---

<objective>
Set up WiX Toolset v5 and create the foundational MSI project structure. This establishes the build infrastructure for creating Windows Installer packages.

Purpose: Provide the technical foundation for building professional MSI installers
Output: Working WiX project that produces a valid MSI file
</objective>

<execution_context>
@C:/Users/samar/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install WiX Toolset SDK and create project structure</name>
  <files>.wix/WhisperKey.Setup.wixproj</files>
  <action>
Create a new WiX v5 project at `.wix/WhisperKey.Setup.wixproj`:

1. Create the `.wix/` directory if it doesn't exist
2. Create `WhisperKey.Setup.wixproj` with WiX v5 SDK references:
   - Use `WixToolset.Sdk` version 5.0.0 or later
   - Target .NET 8.0 (consistent with main project)
   - Set OutputType to "Package" (produces MSI)
   - Include proper build configurations (Debug/Release)
   - Add preprocessor variables for version and branding

3. The project file should:
   - Reference the main WhisperKey.Main project
   - Include WiX extensions: WixUtilExtension, WixUIExtension
   - Define constants for product name, manufacturer, upgrade code
   - Support both x64 architecture (primary) and x86 (fallback)

Do NOT include actual file components yet - just the project skeleton.
Use the WiX SDK-style project format (not old .wixproj format).
  </action>
  <verify>dotnet build .wix/WhisperKey.Setup.wixproj --verbosity minimal 2>&1 | grep -E "(Build succeeded|0 Error)"</verify>
  <done>WiX project file exists and builds without errors, produces valid .wixproj that can be opened in Visual Studio</done>
</task>

<task type="auto">
  <name>Task 2: Create main WiX source file with product definition</name>
  <files>.wix/WhisperKey.Setup.wxs</files>
  <action>
Create `.wix/WhisperKey.Setup.wxs` with the core product structure:

1. Define the Product element with:
   - Id="*" (auto-generate GUID for each build)
   - Name="WhisperKey Voice Dictation"
   - Version from variables (start with 1.1.0)
   - Manufacturer="WhisperKey"
   - UpgradeCode (fixed GUID for upgrades)
   - Language="1033" (English US)
   - Codepage="1252"

2. Set Package attributes:
   - InstallerVersion="500" (Windows Installer 5.0)
   - Compressed="yes"
   - InstallScope="perMachine" (requires admin)
   - Platform="x64" (primary)

3. Create the standard directory structure:
   - ProgramFiles64Folder/WhisperKey/ (install location)
   - ProgramMenuFolder/WhisperKey/ (start menu)
   - DesktopFolder (optional shortcut)

4. Include minimal placeholder components:
   - One directory reference
   - One component group (empty for now)

5. Add MajorUpgrade element for proper upgrade handling:
   - Schedule="afterInstallInitialize"
   - AllowSameVersionUpgrades="no"
   - DowngradeErrorMessage="A newer version is already installed."

Do NOT add file components yet - just the structural skeleton.
  </action>
  <verify>dotnet build .wix/WhisperKey.Setup.wixproj 2>&1 | grep -i "error" | wc -l | grep "^0$"</verify>
  <done>WhisperKey.Setup.wxs exists with valid product definition, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create WiX include files for variables and configuration</name>
  <files>.wix/Variables.wxi, .wix/Config.wxi</files>
  <action>
Create WiX include files to centralize configuration:

**Variables.wxi:**
Define preprocessor variables for:
- ProductVersion (1.1.0)
- ProductName (WhisperKey Voice Dictation)
- Manufacturer (WhisperKey)
- ProductCode (auto-generated placeholder)
- UpgradeCode (fixed GUID)
- BuildNumber (for CI/CD)
- DefaultInstallDir (ProgramFiles64Folder)

**Config.wxi:**
Define conditional compilation symbols:
- DEBUG/RELEASE configurations
- Feature flags for optional components
- Architecture-specific settings (x64 vs x86)
- Logging level configuration

Both files should use the `<?include ?>` pattern and define `<?define ?>` statements that can be referenced in the main .wxs file using `$(var.Name)` syntax.

Update WhisperKey.Setup.wxs to include both files at the top.
  </action>
  <verify>grep -E "\$\(var\.(ProductVersion|Manufacturer|ProductName)\)" .wix/WhisperKey.Setup.wxs | wc -l | grep -E "[3-9]"</verify>
  <done>Variables.wxi and Config.wxi exist, are included in main .wxs, and variables are used in product definition</done>
</task>

<task type="auto">
  <name>Task 4: Update solution file to include WiX project</name>
  <files>WhisperKey.sln</files>
  <action>
Update the main solution file to include the WiX installer project:

1. Add the WiX project to WhisperKey.sln:
   - Project type GUID for WiX: `{B9A3A8B8-6E4A-4B6A-8F6A-6B4E5F3D2A1B}` (or use standard C# project type)
   - Project name: "WhisperKey.Setup"
   - Project path: `.wix\WhisperKey.Setup.wixproj`
   - Solution folder: "Setup" (create if doesn't exist)

2. Set build dependencies:
   - WhisperKey.Setup depends on WhisperKey.Main
   - Build order: Main → Setup

3. Ensure both Debug and Release configurations include the Setup project

4. Update Directory.Build.props to include WiX-specific properties:
   - Add WiX targets import
   - Define shared version properties
   - Configure output paths for MSI

Verify the solution loads correctly in Visual Studio.
  </action>
  <verify>dotnet sln list | grep -i "setup" | grep -i "wix"</verify>
  <done>Solution includes WiX project, builds in correct order, no load errors</done>
</task>

</tasks>

<verification>
- [ ] WiX project builds successfully with `dotnet build`
- [ ] MSI file is produced in output directory
- [ ] Solution loads without errors in Visual Studio
- [ ] Build order is correct (Main before Setup)
- [ ] Variables are properly defined and referenced
</verification>

<success_criteria>
- WiX Toolset v5 SDK integrated into solution
- Working MSI project that produces a valid MSI file
- Solution includes WiX project with proper dependencies
- Build completes without errors in both Debug and Release
</success_criteria>

<output>
After completion, create `.planning/phases/07-msi-installer/07-01-SUMMARY.md`
</output>
