---
phase: 07-msi-installer
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .wix/Components.wxs
  - .wix/Files.wxs
  - .wix/Registry.wxs
  - .wix/Shortcuts.wxs
  - .wix/WhisperKey.Setup.wxs
autonomous: true
must_haves:
  truths:
    - All application files are included in the installer
    - Registry entries for file associations and protocol handlers
    - Start menu and desktop shortcuts are created
    - Component rules are followed (one file per component GUID)
    - Files are harvested from the main project build output
  artifacts:
    - path: .wix/Components.wxs
      provides: Component definitions for all files
      min_lines: 150
    - path: .wix/Files.wxs
      provides: File harvesting and deployment
      min_lines: 100
    - path: .wix/Registry.wxs
      provides: Registry entries for app integration
      min_lines: 80
    - path: .wix/Shortcuts.wxs
      provides: Start menu and desktop shortcuts
      min_lines: 60
  key_links:
    - from: .wix/WhisperKey.Setup.wxs
      to: .wix/Components.wxs
      via: ComponentGroupRef
      pattern: "ComponentGroupRef.*Id=\"ProductComponents\""
    - from: Components
      to: Files
      via: File elements with Source attributes
      pattern: "Source=\"\$\(var\.TargetPath\)"
---

<objective>
Define all installation components: files, registry entries, and shortcuts. This creates the actual payload that gets installed on the user's system.

Purpose: Package the WhisperKey application with all necessary files and system integration
Output: Complete component definitions for all installable items
</objective>

<execution_context>
@C:/Users/samar/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-msi-installer/07-01-SUMMARY.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harvest application files from build output</name>
  <files>.wix/Files.wxs</files>
  <action>
Create `.wix/Files.wxs` to define all files to be installed:

1. Use Heat (WiX harvest tool) concepts to define file harvesting:
   - Reference the main project's publish output
   - Harvest from `$(var.WhisperKey.Main.TargetPath)` directory
   - Include all DLLs, EXEs, and config files

2. Define the file structure:
   - WhisperKey.exe (main executable)
   - All dependency DLLs (NAudio, Serilog, etc.)
   - appsettings.json and other config files
   - Resource files (icons, sounds if any)

3. Use Component elements with proper attributes:
   - Each file gets its own Component with a unique GUID
   - Use `Guid="*"` for auto-generation or fixed GUIDs for tracking
   - Set `Permanent="no"` (allow uninstall)
   - Set `SharedDllRefCount="yes"` for shared DLLs

4. Organize into ComponentGroup "ProductComponents" for easy referencing

5. Include File elements with:
   - Id (unique identifier)
   - Name (filename as installed)
   - Source (path to built file)
   - KeyPath="yes" for the main executable
   - Vital="yes" for critical files

Use preprocessor variables like `$(var.WhisperKey.Main.TargetDir)` for paths.
  </action>
  <verify>grep -c "Component Id=" .wix/Files.wxs | awk '{print ($1 >= 10 ? "pass" : "fail")}'</verify>
  <done>Files.wxs exists with at least 10 components covering all application files</done>
</task>

<task type="auto">
  <name>Task 2: Define registry entries for Windows integration</name>
  <files>.wix/Registry.wxs</files>
  <action>
Create `.wix/Registry.wxs` for system registry entries:

1. Define RegistryKey elements for:
   - Application registration (HKLM\Software\WhisperKey)
   - Uninstall information (already handled by MSI, but add custom values)
   - File associations (if applicable)
   - Protocol handlers (whisperkey:// if needed)
   - Auto-start settings (optional)

2. Include registry values for:
   - Install location path
   - Version information
   - Settings storage location (%APPDATA%\WhisperKey)
   - Default audio device preferences
   - License/compliance flags

3. Create RegistryValue elements with:
   - Type="string", "integer", or "binary"
   - Value (use WiX properties for dynamic values)
   - ForceCreateOnInstall="yes"
   - ForceDeleteOnUninstall="yes"

4. Organize into Component groups:
   - RegistryComponent (system-wide settings)
   - UserRegistryComponent (HKCU settings, deferred to first run)

5. Add registry for Windows context menu integration (optional):
   - Right-click menu entries
   - Shell extensions (if applicable)

Ensure all registry entries use proper root keys (HKLM vs HKCU) based on install scope.
  </action>
  <verify>grep -c "RegistryValue" .wix/Registry.wxs | awk '{print ($1 >= 5 ? "pass" : "fail")}'</verify>
  <done>Registry.wxs exists with at least 5 registry values for app integration</done>
</task>

<task type="auto">
  <name>Task 3: Create Start Menu and Desktop shortcuts</name>
  <files>.wix/Shortcuts.wxs</files>
  <action>
Create `.wix/Shortcuts.wxs` for application shortcuts:

1. Define Directory elements for shortcut locations:
   - ProgramMenuFolder\WhisperKey\ (Start Menu folder)
   - DesktopFolder (desktop shortcut, optional)
   - StartupFolder (optional auto-start)

2. Create Component elements for each shortcut location:
   - StartMenuShortcuts component
   - DesktopShortcut component (with configurable property)

3. Define Shortcut elements with:
   - Id (unique identifier)
   - Name="WhisperKey Voice Dictation"
   - Description="Universal voice dictation for Windows"
   - Target="[INSTALLFOLDER]WhisperKey.exe"
   - WorkingDirectory="INSTALLFOLDER"
   - Icon="WhisperKeyIcon.exe" or separate icon file
   - Arguments (none for main app)
   - Show="normal" (window state)

4. Include RemoveFolder elements for cleanup:
   - Remove Start Menu folder on uninstall
   - Remove Desktop shortcut on uninstall (if created)

5. Add URL shortcuts (optional):
   - Documentation link
   - Support website
   - Release notes

6. Use properties for conditional installation:
   - INSTALLDESKTOPSHORTCUT property (default: 1)
   - Show checkbox in UI for desktop shortcut

Ensure shortcuts reference the correct component containing the executable.
  </action>
  <verify>grep -c "Shortcut Id=" .wix/Shortcuts.wxs | awk '{print ($1 >= 2 ? "pass" : "fail")}'</verify>
  <done>Shortcuts.wxs exists with at least 2 shortcuts (Start Menu + Desktop)</done>
</task>

<task type="auto">
  <name>Task 4: Integrate components into main installer</name>
  <files>.wix/WhisperKey.Setup.wxs, .wix/Components.wxs</files>
  <action>
Create `.wix/Components.wxs` as the master component file and update main .wxs:

1. In Components.wxs, create ComponentGroup "ProductComponents" that references:
   - All file components from Files.wxs
   - Registry components from Registry.wxs
   - Shortcut components from Shortcuts.wxs

2. Use ComponentGroupRef in main .wxs:
   ```xml
   <Feature Id="ProductFeature" Title="WhisperKey" Level="1">
     <ComponentGroupRef Id="ProductComponents" />
   </Feature>
   ```

3. Define Feature tree structure:
   - Main Feature (required, Level 1)
   - DesktopShortcut Feature (optional, Level 1, configurable)
   - Documentation Feature (optional, Level 2)

4. Add Condition elements for optional features:
   - Desktop shortcut condition based on property
   - OS version checks (Windows 10/11 only)
   - Architecture checks (x64 required)

5. Update main .wxs to include all fragment files:
   ```xml
   <?include Files.wxs?>
   <?include Registry.wxs?>
   <?include Shortcuts.wxs?>
   ```

6. Verify component rules:
   - Each component has one key path
   - No two components share the same key path
   - GUIDs are unique
   - Files don't conflict with Windows system files

Run heat.exe or equivalent to validate file harvesting works.
  </action>
  <verify>dotnet build .wix/WhisperKey.Setup.wixproj 2>&1 | grep -E "(error|warning)" | wc -l | grep "^0$"</verify>
  <done>All components integrated, build succeeds without errors or warnings</done>
</task>

</tasks>

<verification>
- [ ] All application files are defined in Files.wxs
- [ ] Registry entries cover app registration and integration
- [ ] Shortcuts are created in Start Menu and optionally Desktop
- [ ] ComponentGroup "ProductComponents" references all items
- [ ] Build produces MSI with all components
- [ ] No WiX validation errors (ICE errors)
</verification>

<success_criteria>
- Complete file harvesting from build output
- Registry entries for Windows integration
- Start Menu and Desktop shortcuts defined
- Feature tree supports optional components
- Build completes with all components included
</success_criteria>

<output>
After completion, create `.planning/phases/07-msi-installer/07-02-SUMMARY.md`
</output>
